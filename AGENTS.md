# 橋本勇太 / Hashi — Works 個人サイト 引き継ぎドキュメント

理学療法を学ぶ橋本勇太さん本人のポートフォリオサイト。2026-09-19 構築・公開。
このファイルは Codex 等の別エージェントへの引き継ぎ用。

- **公開URL**: https://celties.github.io/hashi-portfolio/
- **リポジトリ**: https://github.com/celties/hashi-portfolio （public）
- **ローカル**: `/Users/celties/Downloads/Claude/hashi-portfolio/`

---

## 0. 最初に読むべき要点

1. **これは本人の経歴を載せる公開サイト。書いてあることは全部「事実の主張」になる。** 読みやすさのために断定形へ書き換えない（§4）
2. **本人が明示的に決めた「載せない」ルールが4つある。**（§3）破ると本人の不利益になる
3. **内容はほぼ全部 `data.js` にある。** 文章と作品の追加はそこだけ触れば済む。HTML/CSS/JS の構造を変える必要はほぼない
4. **ビルド不要・依存パッケージゼロ。** `git push` するだけで公開サイトが更新される（§6）
5. **Claude の過去の失敗が3回ある。** 同じことを繰り返さないよう §4 を読むこと

---

## 1. 構成

```
index.html   マークアップ（セクションの骨組みだけ。中身はJSが描画）
style.css    スタイル（ダークネイビー #12233A × オレンジ #F2622E）
data.js      ★ 全コンテンツ。プロフィール・作品17本・ルーツ・趣味・
             影響を受けた作品・強み・大学での学び・経験・進路・年表・スキル
app.js       描画・カテゴリー絞り込み・モーダル・スクロール演出
pixel.js     ドット絵キャラ（33×48の文字列マップ → SVG生成）
AGENTS.md    このファイル
```

ビルドツール・npm・フレームワークは一切使っていない。HTMLを開けば動く。

### セクション構成（この順番は本人の指定）

```
01 About      プロフィール / 生まれた町（笛吹市） / ルーツ年表 / 診断 / ドット絵
02 Interests  好きなこと6 / 影響を受けた2作品 / 強み5
03 Study      大学での4つの探究 / 副専攻の実験 / 通底する3つの軸
04 Career     活動とアルバイト / エッセイ / NBAトレーナー / 積んでいること5項目
05 Works      作品17本（5カテゴリで絞り込み、カードから詳細モーダル）
06 Timeline   2026年4月からの半年
07 Skills     5グループ
08 Contact
```

**人物が主役で、作品はその裏づけ**という並び。2026-09-19 に本人の指示で
Works を後ろへ移した。この順番を勝手に戻さないこと。

---

## 2. コンテンツの直し方

### 文章を変える

`data.js` の該当箇所を書き換えるだけ。HTMLには文章が入っていない。

### 作品を1本足す

`PROJECTS` 配列の先頭にオブジェクトを追加する。カード・件数・絞り込み・
詳細モーダルすべてに自動で反映される。

```js
{
  id: "new-project",          // 一意な文字列。HTMLのid属性と衝突させないこと（§5）
  title: "作品名",
  subtitle: "ひとことで言うと",
  category: "habit",          // habit | basketball | video | automation | study
  period: "2026.10",
  featured: false,            // true にすると2カラム幅の大きいカードになる
  emoji: "✨",
  summary: "カードに出る要約（3行で切れる）",
  body: ["モーダルの本文", "段落ごとに配列の要素にする"],
  tech: ["Next.js", "TypeScript"],
  links: [{ label: "公開サイト", href: "https://example.com" }],
  highlights: ["特徴を箇条書きで"]
}
```

### ドット絵を直す

`pixel.js` の `BODY` は 33×48 の文字列マップ。1文字が1ドット。
色は同ファイルの `PAL` で決まる。

| 文字 | 色 |
|---|---|
| `H` | 髪 |
| `O` | オレンジ（ヘッドバンド・胸のTMU） |
| `S` | 肌 |
| `K` | 目・口 |
| `N` | ネイビーのウェア |
| `W` | 白（ショーツ・シューズ） |
| `G` | ソール |
| `B` / `D` | ボールとその縫い目 |

**各行の長さを33に保つこと。** 崩すと絵が破綻する。
まばたきは `EYE_ROWS` / `EYE_COLS` で、目の上下だけ肌に戻して線を残す方式。
頭上の「BELIEVE」の貼り紙は CSS（`.px-believe`）で作っている。

---

## 3. 本人が決めた「載せない」ルール（重要）

いずれも本人が明示的に指示したもの。**勝手に戻さないこと。**

1. **志望企業14社の実名は書かない。** 3つの方向性の説明のみ
2. **実家の手伝い（注文書読み取りシステム）は、屋号・リポジトリ名・アプリURLを
   一切書かない。リンクも張らない。** 「実家の手伝い（ボランティア）」とだけ記載する
3. **作品に「稼働中」などの状態表記を持たせない。** 時間が経つと嘘になるため、
   バッジは期間とカテゴリーのみ。`status` フィールドは削除済み
4. **未確定のことは「予定」と明記する。** 例: 「2028年3月卒業予定」

### 関連する落とし穴

`celties/naegi-order` は **Streamlit Community Cloud にデプロイされ、実家の人が
日常的に使っている。private にしてはいけない。** Streamlit はリポジトリから直接
アプリを配信するため、private 化すると起動できなくなる。
（2026-09-19 に一度 private にして、public へ戻した経緯がある）

屋号を伏せたいのは**このサイト側だけ**。naegi-order のコード内には屋号が9か所以上
あり、画面表示にも使われているので消せない。

---

## 4. Claude が実際にやらかした失敗（繰り返さないこと）

本人の経歴を扱うため、**盛る方向の間違いが致命的**になる。3回発生した。

| 誤り | 実際 |
|---|---|
| 「スウェーデン短期研修に**参加した**」 | まだ**計画中**。事前学習の段階 |
| 「5分間の学術発表に**まとめた**」 | 発表に向けて**構成案を作成**した段階 |
| 「**TOEIC L&R IPを受けた**」 | **未受験** |

**原因**: 本人が渡してくる整理メモは「やったこと」と「やる予定のこと」を同じ
見出しの下に並べていることがある。読みやすさを優先して完了形に変換すると事故る。

**対策**: 「〜に向けた」「準備」「参加・準備」のような語を見たら、完了形にせず
**本人に確認する**。面接で掘られて食い違うと、他の記述の信用まで落ちる。

もうひとつ。**推測で埋めない。** 学部名・在住地・所属の有無などを一般知識で
補完すると、本人が気づかないまま嘘が残る。不明なら空けて本人に聞く。

---

## 5. 技術的な落とし穴

- **HTMLのid属性とJSのgetElementByIdが衝突する。** `<section id="career">` と
  `<div id="career">` を同時に作ると `getElementById` がセクションを返し、
  `innerHTML` でセクションの中身を全部消す事故が起きた。コンテナのidは
  `careerGroups` のようにセクションidと別名にすること
- **`.sec` に `position:relative` が付いている**ため、`element.offsetTop` は
  ドキュメント基準にならない。スクロール位置の計算には `getBoundingClientRect()` を使う
- **ブラウザのキャッシュが強い。** CSSやJSを直したら `index.html` の
  `?v=NN` を必ず上げること（現在 v=32）。上げ忘れると反映されない
- 差し込みは全て `esc()` を通す。`textContent` 代入は例外

---

## 6. 更新手順

```bash
cd /Users/celties/Downloads/Claude/hashi-portfolio
# data.js などを編集
git add -A && git commit -m "更新内容" && git push
```

push の1分ほどで https://celties.github.io/hashi-portfolio/ に反映される。
GitHub Pages は main ブランチのルートを配信する設定（build_type: legacy）。

### ローカル確認

```bash
python3 -m http.server 8790
```

`open index.html` でも動く（相対パスのみのため）。

### 公開をやめる

リポジトリを private にすればサイトは消える。復旧は public に戻すだけ。

---

## 7. プライバシー・セキュリティの状態（2026-09-19 監査済み）

- 載っている個人情報は**本人のもののみ**。第三者の氏名・連絡先はゼロ
- 入力フォーム・Cookie・localStorage・外部送信コード・アクセス解析：**すべてゼロ**
- 外部への接続は **Google Fonts のみ**（本人が了承済み）。自前配信に切り替えることも可能
- git履歴に鍵・トークンの混入なし
- 外部リンクは全件 `rel="noopener"` 付き

**新しくフォームや解析タグを足すときは、本人に確認してから。**

---

## 8. 未確認のまま残っていること

- アルバイトを「学生時代の」とまとめてよいか（焼肉屋3年＋病院2年で計5年分あり、
  大学2年半に収まらない。高校時代分が含まれる可能性）
- 「ボストン・セルティックス**一筋**」と言い切ってよいか（図鑑アプリを作った事実からの推測）
- 拠点「東京」（都立大に通っている事実からの推測。実家は山梨）
- 学部名「健康福祉学部」（一般知識で補完。本人未確認）
